package fr.diginamic.geoff.mapper;

import fr.diginamic.geoff.dto.PlayerDTO;

public class PlayerDTOMapper
{
    public PlayerDTO mapToEntity(String l) {
        PlayerDTO playerDTO = new PlayerDTO();
        return playerDTO;
    }
}